- title "Story"

.float_right= h @story.published_on_formatted
%h4
  = h @story.title
  - if admin?
    = link_to 'Edit', edit_story_path(@story)
    = link_to 'Destroy', @story, :confirm => 'Are you sure?', :method => :delete

%div
  = textilize @story.body

- if admin?
  %p
    = link_to "View All", stories_path
